# Goal 4 — AI-Searchable Knowledge Hub: Working Checklist

A living to-do list derived from [goal4-knowledge-hub-spec.md](goal4-knowledge-hub-spec.md) and [goal4-implementation-plan.md](goal4-implementation-plan.md). Check items off as they're done, edit freely as things change.

**Last updated:** 2026-09-14 — Phases 2 and 3 are both fully done, including the real citation-format fix (`scripts/fluffyjaws-citations.js`) verified against an actual captured stream. **Nothing is committed to git yet** — still local uncommitted changes on the stray `docs/goal4-knowledge-hub-concept` branch. Next: commit/PR, place the blocks on the homepage/hub docs, then run Phase 4 end-to-end validation for real.

## Phase 0 — Decisions

- [x] Audience confirmed: Adobe SSO only, same as the blog (no anonymous/public support needed)
- [x] ~~v1 scope confirmed: blog + AI CoC write-ups only; transcripts deferred to phase 2~~ — **superseded (2026-09-04): transcripts are now in scope for v1.** See Phase 2 below.
- [ ] Confirm target Q4 (which year) and any interim checkpoint
- [ ] Decide who owns the FluffyPack(s) day-to-day and who's the point of contact for platform follow-ups

## Phase 1 — Platform-team engagement ✅ CLOSED

- [x] Sent questions to Thomas Cantonnet and `#fluffyjaws-enablement` (informally, via Slack + FluffyJaws itself, rather than one formal batch)
- [x] Answer received: browser-direct vs. backend — **resolved: browser-direct, via Okta SPA + PKCE, no backend needed**
- [x] Answer received: can a SharePoint source be scoped to subfolders? — **resolved: folder-level registration works, but no exclude-subfolder rule exists; `Transcripts/` must be physically moved out before registering the parent folder**
- [x] Answer received: refresh/re-index cadence — **resolved: asynchronous, ~daily/overnight, no confirmed manual trigger; expect up to ~24h freshness lag**
- [x] Answer received: query-time source filter, or three FluffyPacks? — **resolved: no query-time filter exists; build three separate packs**
- [x] Answer received: request/response schema for the stream API — **partially resolved: full request/response field reference confirmed; structured citation schema NOT confirmed — must be verified empirically in Phase 3 (see below)**
- [x] Answer received: browser/session auth + CORS/origin requirements — **resolved: CORS is per-registered-integration; registered origins may send credentialed token-based requests to `/api/v1/*`**
- [x] Answer received: usage metrics/exports beyond aggregate volume — **resolved: three per-integration export routes exist (`/feedback/agent`, `/feedback/fluffypack`, `/fluffypack/insights`), aggregates/metadata only, granted on request**
- [x] Answer received: rate limits, latency, cost/chargeback model — **resolved: tens of chat requests/min per identity, no `Retry-After` header, 30s-to-first-event / 90s-idle timeouts, 20MB/file & 30MB/request limits; cost/chargeback model not confirmed (low priority, revisit only if usage becomes a concern)**

## Phase 2 — Content sources & FluffyPack setup — UNBLOCKED, start here

**Decision (2026-09-04):** holding off on adding the `knowledge-search` block to the homepage doc (`Shared Documents/content/index.docx`, per `fstab.yaml`'s mount) until this phase is done — no point previewing a search box that's guaranteed to fail at the FluffyJaws call with no pack to query. Once a real pack exists, add a table with `Knowledge Search` as the first cell's text anywhere on the homepage doc — that's the whole authoring step, EDS maps the name to `blocks/knowledge-search/` automatically. This is a SharePoint/Word editing step outside Claude's tool access in this session — Dominik does this part.

**Decision change (2026-09-04): transcripts are now in scope for v1** (previously deferred to Phase 6). `Transcripts/` stays nested under `content/en/aicoc/` — **no folder move needed**, since FluffyJaws registration is recursive (the same behavior that was a problem for *excluding* transcripts now works in our favor for *including* them). What still needs to happen first, per the decision to do prep before registering (not backfill after): rename each transcript to `<write-up-slug>-transcript.<ext>` and add a `Source session write-up: <url>` line near the top of each doc, **before** registering `content/en/aicoc/` — otherwise transcript-sourced answers risk citing the raw transcript file instead of the write-up page. Old Phase 6 below is superseded by this — folded in here instead.

- [x] **Done and verified (2026-09-11):** 39 session transcripts (the original ~34 plus a few added since, including a late-arriving "Ad Hoc ... Trent Davis" session) all have the `Source session write-up: <url>` annotation line added — full mapping in [goal4-transcript-rename-mapping.md](goal4-transcript-rename-mapping.md). **Scope simplified along the way:** transcript filenames themselves turned out not to matter (FluffyJaws indexes content, not filenames — the annotation line is what actually drives correct citations), so those were left as whatever they ended up at, not force-renamed to a strict convention. What *does* matter is that all 10 write-ups that were still under raw, unslugged filenames got renamed to their clean slug (`content/en/aicoc/*.docx`, verified directly against the live SharePoint folder) — since a write-up's filename is what determines its actual published URL, this was required for those 10 annotation URLs to be real, not optional tidiness.
  - ⚠️ **Not yet published:** those 10 renamed write-ups need Preview + Publish (via AEM Sidekick, bulk multi-select support in the SharePoint file browser) before their new URLs go live. Decided **not** to unpublish the old stale URLs for now — low-risk to leave as orphaned pages, cleanup deferred, doesn't block anything else. This doesn't block FluffyPack registration either (FluffyJaws reads the SharePoint source directly, not the published site) — only matters for citations actually resolving when clicked (Phase 4 exit criterion).
- [x] **Done (2026-09-11):** registered `content/en/publish/` and `content/en/aicoc/` as FluffyJaws sources (both classified "Not sensitive", "Include subfolders" so `aicoc`'s `transcripts/` gets swept in), via the main FluffyJaws app's "Attach exact SharePoint file" source type (accepts folder scopes despite the name).
  - ⚠️ **New prerequisite discovered, not in the original plan:** FluffyJaws reads SharePoint via a service account, `aemcswrkspce@adobe.com` — both folders had to be explicitly shared with that account first, or validation/indexing is denied. Done for both `publish/` and `aicoc/`.
  - Source URLs used: `https://adobe.sharepoint.com/sites/InsideAEM/Shared Documents/content/en/publish` and `.../content/en/aicoc`.
- [x] Created all three FluffyPacks: **`inside-aem-all`** (both sources), **`inside-aem-blog`** (publish only), **`inside-aem-aicoc`** (aicoc only). Confirmed each pack's "Legacy vanity" slug exactly matches its Name (e.g. `inside-aem-all`) — matches what `PACK_BY_SCOPE` in both search blocks already hardcodes, no code changes needed. Access left at the default "Everyone at Adobe", matching our earlier access decision.
  - **Side discovery:** an existing pack `Re-Think | AI | Culture-tecture` (owned by a different user, `sil17145`) already indexes our blog independently — possible overlapping effort, worth a quick check-in with them, not blocking.
  - ⚠️ **Bug found and fixed (2026-09-14):** despite the creation wizard visually confirming sources were added, both `inside-aem-blog` and `inside-aem-aicoc` came out of creation with **0 searchable sources actually saved** (confirmed via each pack's own overview page, "Knowledge: NOT ADDED"). Only `inside-aem-all` was correctly saved. Fixed by re-adding each pack's source directly via its own **Edit → Knowledge** page (not the creation wizard) — both now correctly show "1 searchable source · READY". **Lesson for next time:** always verify a pack's Knowledge card on its own overview page after creation — the wizard's in-progress display isn't reliable proof it persisted.
- [x] **Verified (2026-09-14), directly in the FluffyJaws chat UI:** ingestion is done and ~3 days old at this point (well past the ~24h estimate — the earlier 0-sources bug was the actual blocker, not ingestion timing). `inside-aem-blog` and `inside-aem-aicoc` each return real, correctly-scoped titles (blog pack → only blog posts; AI CoC pack → only AI CoC sessions) — confirms scoping genuinely works, not just "some pack answers something." `inside-aem-all` (2 sources, confirmed via its own overview page) not yet chat-tested directly but its sources were never in doubt.
  - Real citation-format finding from this test — see the write-up-vs-transcript citation gap noted under Phase 3's citation-capture task below.
  - **`inside-aem-all` also verified (2026-09-14):** asked for a mix of blog + AI CoC content, got a correctly balanced 3+3 response pulling from both sources — combined pack works as intended. **Phase 2 is now fully done.**

## Phase 3 — Build the search UI

- [x] Register a new Okta application at `https://oss.corp.adobe.com/okta/` — OpenID Connect, **Single Page Application** type, PKCE, no client secret, redirect URI set to the site's callback
  - App name: `inside-aem-knowledge-hub` · Data Classification: Confidential · Environment: Production
  - **Client ID:** `0oa28nqie1kVri8670h8`
  - **Issuer (production):** `https://adobe.okta.com`
  - **Authorize:** `https://adobe.okta.com/oauth2/v1/authorize` · **Token:** `https://adobe.okta.com/oauth2/v1/token` · **Keys (JWKS):** `https://adobe.okta.com/oauth2/v1/keys`
  - **Redirect URIs registered (4):** `https://culture-tecture.adobe.com/`, `https://re-think.adobe.com/`, `https://culture-tecture.adobe.com/en/aicochub`, `https://re-think.adobe.com/en/aicochub` — redirects back to the originating page itself rather than a dedicated `/auth/callback` route
  - [x] **Fully resolved and verified (2026-09-01):** Support ticket (see [goal4-iam-support-ticket.md](goal4-iam-support-ticket.md)) answered by Harpreet Singh (GOC, ATS) — the Okta app's own "Application Access" field is now set to `All` (confirmed directly on the app's OIDC settings page). This is at the **app level**, not the `GRP-INSIDE-AEM-KNOWLEDGE-HUB-USERS` group (which still shows only 1 member and is no longer relevant to access control here — safe to ignore/leave as-is).
  - [x] **Verified with a real manual OAuth test** (2026-09-01): hand-built an Authorization Code + PKCE `authorize` URL (fresh code_verifier/challenge, `redirect_uri=https://culture-tecture.adobe.com/`) and opened it in-browser. Result: no visible Okta login/consent screen (already-signed-in session carried through silently), landed back on `https://culture-tecture.adobe.com/?code=mcaMGrVwrf5d0s8qoWmfhCI54hywaN4vouCd8ql0Id0&state=...` with the `state` matching exactly. **Confirms the full PKCE flow works end-to-end, and that silent auth for already-signed-in visitors just works with no `prompt=none` or extra handling needed.**
- [x] Register that Okta client as a FluffyJaws integration, with `culture-tecture.adobe.com` and `re-think.adobe.com` as CORS origins
  - Registered at `https://fluffyjaws.adobe.com/integrations/apps` (separate tool from the data-source/bot-config portal). App: **Inside AEM Knowledge Hub** (`inside-aem-knowledge-hub`), status **ACTIVE**, owner dsteinacher (primary) — Stefan can be added as a co-owner once he's signed into FluffyJaws at least once.
  - **Production API host confirmed:** `https://api.fluffyjaws.adobe.com/api/v1/stream`
  - **Auth reminder (per FluffyJaws contact, 2026-09-01):** the FluffyJaws registration doesn't need our Okta issuer — that stays in our own browser OAuth config. Use `Authorization: Bearer <per-visitor Okta access token>` obtained via our own SPA's Authorization Code + PKCE flow against `https://adobe.okta.com`; do **not** reuse the separate custom authorization-server endpoint documented for the service-token/`client_credentials` flow — that's for a different auth path entirely.
- [x] Build the OAuth Authorization Code + PKCE login flow into the search UI (code side)
  - [scripts/fluffyjaws-auth.js](../scripts/fluffyjaws-auth.js) — `login()`, `handleRedirectCallback()` (state check, token exchange, full ID-token validation per the Okta compliance checklist), and the public entry point `getAccessToken()`. `redirect_uri` is computed from the current page (origin + pathname) rather than hardcoded, so it works on all 4 registered pages automatically.
- [x] ~~Test whether silent/`prompt=none`-style auth works for visitors already signed into Okta~~ — **confirmed working above, without even needing an explicit `prompt=none` param.** Still worth handling the "not already signed in" case gracefully (visible redirect) in code, but the happy path needs no special silent-auth trick.
- [x] Build compact search block for the homepage (question in → streamed answer → "view sources")
  - [blocks/knowledge-search/](../blocks/knowledge-search/) — single-turn, scope selector (All/Blog/AI CoC → `inside-aem-all`/`inside-aem-blog`/`inside-aem-aicoc`), site's own indigo accent (not AI CoC red — that's scoped to the hub). Persists the in-flight query to `sessionStorage` before calling `getAccessToken()` so a search interrupted by an Okta login redirect resumes automatically on return, without forcing a login just from visiting the homepage.
  - **Verified 2026-09-04** with a throwaway local harness (not committed): block renders correctly matching site styling (confirmed via screenshot), no console errors, and submitting a query correctly triggered the real `getAccessToken()` → `login()` → redirect to `adobe.okta.com` with zero errors beforehand — confirms the block wires into the already-proven PKCE flow correctly. (Full round-trip completion isn't testable from `localhost` since it's not a registered redirect URI — already proven separately on the real domain, see the Okta app entry above.)
  - **Still using the unverified `event.delta` field-name assumption** and the narrow own-domain-URL citation regex — both flagged in code comments, both waiting on the real-stream-capture task below.
- [x] Build fuller search block for the AI CoC hub (source cards, scope selector across the three packs)
  - [blocks/knowledge-hub-search/](../blocks/knowledge-hub-search/) — multi-turn (client-side `history` array, resent in full each turn per the API guide's own recommendation), scope defaults to `aicoc` (per spec §2/§8), switching scope mid-conversation starts a fresh transcript (mixing packs across turns would be semantically odd). Red/black/white AI CoC branding (`--khs-red: #eb1000`), explicitly overriding the site-wide indigo `button` style — same convention `.session-feed-load-more` already uses on this page.
  - **Source cards, not just links:** upgrades a bare citation URL into a real card (title + AI CoC/Blog kind + presenter/author) when it matches the site's own `/en/aicoc-index.json` or `fetchBlogArticleIndex()` (reused from `scripts/scripts.js`, same as `article-feed.js`) — falls back to a plain link otherwise.
  - Same interrupted-login-resume pattern as the homepage block, but restores the **entire transcript**, not just the in-flight question, since a redirect could interrupt any turn, not only the first.
  - **Verified 2026-09-04** with a throwaway local harness: confirmed rendering (scope selector, chat layout, red/black/white branding) via screenshot, confirmed the real `getAccessToken()` → `login()` redirect fires correctly on submit, and separately verified the transcript/source-card CSS by injecting sample markup matching the block's real structure. One harness gotcha worth remembering: importing `fetchBlogArticleIndex` from `scripts/scripts.js` runs the *entire* EDS page bootstrap as a side effect (same as `article-feed.js` relies on in production) — a standalone test harness needs to keep its mount point outside `<main>` to avoid the bootstrap overwriting it.
  - Same two placeholders as the homepage block (`event.delta` field name, narrow own-domain citation regex) — flagged in code comments, waiting on the real-stream-capture task below.
- [x] Wire both to call `/api/v1/stream` directly from the browser with the per-visitor Okta user token — no backend proxy
  - [scripts/fluffyjaws-client.js](../scripts/fluffyjaws-client.js) — hand-rolled SSE parsing (`EventSource` can't do POST/custom headers), 30s-to-first-event / 90s-idle watchdog timeouts, `FluffyJawsError`/`isRateLimited()` for error handling, and an `onRawFrame` hook for the raw-stream-capture task below. Now consumed by both `knowledge-search` and `knowledge-hub-search`.
  - **Real request/response schema now confirmed** via the official FluffyJaws API Guide (PDF reviewed 2026-09-04, `fluffyjaws.adobe.com/docs/api`): `messages` array (`{role, content}`) is the only required field; `fluffyPackSlug`/`fluffyPackUuid` scope the pack; resend the full transcript each turn rather than relying on `previousResponseId` (simpler, self-contained, matches the guide's own recommendation for typical clients). **A stream failure still returns HTTP 200** — the `error` SSE event is the real failure signal, not the status code.
- [x] **Done and verified (2026-09-14):** captured a real raw SSE stream from the live `inside-aem-aicoc` pack — obtained a real access token via the manual PKCE trick (same as the earlier Okta test) and called `/api/v1/stream` directly with `curl`. Full transcript: 417 frames, ~650KB, ~49s end-to-end (the model used `custom_search` + high reasoning effort — confirms the API guide's "can legitimately run for minutes" warning is real, not hypothetical).
  - **`event.delta` field name confirmed correct** — no code change needed, our original inference matched exactly (`{"type":"response.output_text.delta","delta":"..."}`).
  - **`annotations` (the OpenAI-style structured citation slot) is present but always empty `[]`.** Confirms citations are text-embedded only, never structured — the plan's original caution was right.
  - **The `custom_search` tool's raw results do carry a structured `url` field** (`https://adobe.sharepoint.com/sites/InsideAEM/Shared Documents/content/en/aicoc/<filename>.docx`), but that's tool-internal data the model sees, not something exposed to us as the API caller — we only ever see the model's final free-text answer.
  - **The model cites the raw SharePoint document URL in the final answer text, for both write-ups AND transcripts** — confirmed with hard evidence this time, not just the earlier chat-UI observation. The transcript's retrieved content chunk showed our `Source session write-up:` annotation line arriving **mangled** (`".../en/aicoc/assisted-coding-in-practice ..."` — missing the `https://culture-tecture.adobe.com` prefix), which likely explains why the model had nothing valid to cite instead and fell back to the raw file.
- [x] **Fixed (2026-09-14):** new shared module [scripts/fluffyjaws-citations.js](../scripts/fluffyjaws-citations.js) — `resolveCitations(text)` recognizes both our own public-domain links AND raw `adobe.sharepoint.com` doc URLs, derives a slug from the filename (stripping `.docx` and a trailing `-transcript`), and resolves it to the real public URL via the site's own blog/AI CoC indexes (the same lookup the hub block already had, now shared). Verified against the actual captured stream: both the write-up's and the transcript's raw SharePoint citations for the same session correctly resolve to the identical `culture-tecture.adobe.com/en/aicoc/assisted-coding-in-practice` URL and dedupe into one source. Both blocks now use this shared module instead of their own local (and now-obsolete) `extractSources()`/`buildSourceIndex()` copies — homepage block gained real title resolution for free as a side effect, not just bare links anymore.
- [x] Style to match Inside AEM's brand (Adobe red/black/white) — done as part of building both blocks (see Phase 3 entries above); no separate pass needed.

## Phase 4 — Validate before launch (POC exit criteria)

- [ ] Citations are clickable and land on the correct page
- [ ] Scoped search (Blog / AI CoC / Both) returns distinctly different results
- [ ] Same FluffyPack(s) work from both the homepage and hub placements
- [ ] Freshness measured against the known ~daily/overnight baseline — same-day (not instant) visibility is the pass condition
- [ ] Rate-limit and retry behavior measured under a burst of searches (plan for tens of requests/min per identity)
- [ ] Aggregate FluffyJaws usage view confirmed visible for the registered app
- [ ] Citation rendering matches what was actually observed in the captured SSE stream, not a doc-assumed schema
- [ ] Validate: a transcript-sourced answer actually cites the write-up URL, not the raw transcript file — *(moved from old Phase 6, now that transcripts ship in v1)*

## Phase 5 — Analytics

- [ ] Add a "Knowledge Hub" section/tab to the existing Blog & AI CoC dashboard
- [ ] Instrument: query submitted, scope selected, no-result state, citation clicked, search-to-source click-through
- [ ] Instrument: auth success/failure, latency, rate-limit/error responses
- [ ] Request access to the per-integration feedback/insights export routes from the FluffyJaws team (`/feedback/agent`, `/feedback/fluffypack`, `/fluffypack/insights`) for query-level reporting beyond aggregate volume
- [ ] Confirm monthly searches/users can actually be reported from this combined data

## Phase 6 — Transcripts — SUPERSEDED (2026-09-04)

~~Originally: transcripts held out of v1, added in a later phase.~~ Decision changed — transcripts are in scope for v1. The rename/annotation/registration work that used to live here has moved up into **Phase 2** (transcripts ship in the same `content/en/aicoc/` source and `inside-aem-aicoc`/`inside-aem-all` packs as the write-ups, not as a separate source). The one thing still worth validating stays relevant, just moved to Phase 4:

- [ ] *(moved to Phase 4)* Validate: a transcript-sourced answer actually cites the write-up URL, not the raw transcript file

## Phase 7 — Launch

- [ ] Final review with Stefan (and anyone else who needs to sign off)
- [ ] Go live on the homepage and AI CoC hub
- [ ] Start tracking monthly searches/users against the Q4 success measure
